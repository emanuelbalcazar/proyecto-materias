(function() {
  'use strict';

  angular.module('navigationBar')
  .controller('NavController', ['$scope', NavController]);
  // Controlador utilizado para los manejos de la barra de navegacion
  function NavController(vm) {
    // vm.title = vm.$parent.message;}
  }
})()
